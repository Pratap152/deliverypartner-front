import React from 'react';
import { View, Text } from 'react-native';

export const EmptyState = () => (
  <View style={{ alignItems: 'center', marginTop: 80 }}>
    <Text>No deliveries today</Text>
  </View>
);
